The red boat sat in the green water, and the amber light turned gold.
None of those is a colour literal; they are prose, and the K4 census counted
them as literals until the quoting rule was added.

Colour theory: red and green are complements. This sentence defeated an
earlier fix that only required a preceding colon, so it stays as a control.

A link to a fragment that reads as a hex colour: [top](#abc), and another
[section](#fed).

Prose naming a CSS property: white-space, and a word that merely starts with a
colour name: redirect, blackboard, greenfield.

<Note href="#dad" />
